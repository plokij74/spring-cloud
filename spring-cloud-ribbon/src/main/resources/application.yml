server:
  port: 8444
spring:
  application:
    name: ribbon-consumer
eureka:
  instance:
    port: 8111
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/